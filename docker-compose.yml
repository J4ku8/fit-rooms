 version: '3.7'
 services:

   dashboard:
     env_file:
       - ./.env
     build:
       context: ./
     container_name: docker-dash
     restart: always
     ports:
       - 8050:8050
     networks:
       - network

 networks:
   network:
     driver: bridge